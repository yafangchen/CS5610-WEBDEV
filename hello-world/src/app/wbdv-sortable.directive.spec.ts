import { WbdvSortableDirective } from './wbdv-sortable.directive';

describe('WbdvSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new WbdvSortableDirective();
    expect(directive).toBeTruthy();
  });
});
